import React from "react";

function UserSearch({postData, setPostData}) {
    const currentUser = parseInt(sessionStorage.getItem("user_id"))

    return (

        //Backend working via: /search/${username}
        //Test
        <></>
    )
}

export default UserSearch